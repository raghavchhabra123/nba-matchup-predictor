# Only needed to REBUILD the models (scripts/). The app itself runs on
# requirements.txt alone (models are prebuilt and committed).
-r requirements.txt
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
pyarrow>=14
