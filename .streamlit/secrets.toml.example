# Copy this file to `.streamlit/secrets.toml` and paste your own key.
# Free key: https://console.groq.com  (Streamlit's .gitignore keeps secrets.toml out of git.)
# On Streamlit Community Cloud, add the same line under: App → Settings → Secrets.

GROQ_API_KEY = "your_groq_api_key_here"
